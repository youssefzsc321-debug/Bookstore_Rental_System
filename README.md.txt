# Bookstore & Rental System

A bookstore application for selling and renting books built with **Python (Tkinter + SQLite)**.  
It includes user login, registration, sales/rental operations, book catalog, and PDF invoice export.

## 📦 Requirements
```bash
pip install -r requirements.txt
